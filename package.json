{
  "name": "test-task-counter",
  "version": "1.0.0",
  "description": "Test task DRIVEBACK by Eugene Kuznetsov",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf ./dist && mkdir dist",
    "build": "npm run clean && babel ./src -d ./dist",
    "start": "node ./dist/index.js"
  },
  "author": "Eugene Kuznetsov",
  "license": "ISC",
  "engines": {
    "node": ">=8"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0"
  },
  "dependencies": {
    "express": "^4.16.4"
  }
}
